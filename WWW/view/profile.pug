doctype html
html
    head 
        title DuckLabs - #{teammate.name}
        meta(charset='utf-8')
        link(href="https://fonts.googleapis.com/icon?family=Material+Icons", rel="stylesheet")  
        link(rel='stylesheet', href='https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/css/materialize.min.css')
        link(rel="shortcut icon", href="/images/duck-logo-1.png")
        style 
            include style.css
    body(class="with-background")
        div(class="container pt-100")
            a(class="waves-effect waves-light btn amber darken-1" href="/") Back 
                i(class="material-icons right") keyboard_return
            div(class="card grey darken-3 mt-50 mb-50 noselect ptb-30")
                div(class="center-align")
                    div(class="image-circular", style=`background-image: url(${teammate.imageRoute});`)
                        
                    div(class="card-title white-text")
                        h1 #{teammate.name}
                        each language in teammate.languages
                            div(class="chip") #{language}
                    div(class="card-content white-text")
                        p(class="flow-text") #{teammate.description}
                        div(class="pt-100")
                            object(data=`${teammate.curriculumRoute}` type="application/pdf" width="90%" height="800px")
                                p It appears you don't have a PDF plugin for this browser. No biggie... you can 
                                    a(href=`${teammate.curriculumRoute}`) click here to download the PDF file.


        script(src='https://cdnjs.cloudflare.com/ajax/libs/materialize/1.0.0/js/materialize.min.js')